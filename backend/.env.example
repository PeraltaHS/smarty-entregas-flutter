# Copie este arquivo para ".env" e preencha com os dados do seu banco
# Exemplo:
# no cmd: "cd backend" e depois "copy .env.example .env"

DB_HOST=localhost
DB_PORT=5432
DB_NAME=
DB_USER=postgres
DB_PASS=
